{
  "name": "clock-of-primes",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "typecheck": "tsc --noEmit",
    "build": "npm run typecheck && node scripts/build.mjs",
    "test": "npm run build && node tests/core.cjs && node .review/verify-riemann-math.cjs && node tests/xi-arm.cjs && node tests/theta-range.cjs"
  },
  "engines": { "node": ">=20.18" },
  "devDependencies": { "typescript": "5.9.3", "vite": "6.4.3" }
}
